{
  "name": "entitlement-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4"
  }
}
